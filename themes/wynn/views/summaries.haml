- unless pages.empty?
  .posts
    - pages.each do |page|
      %article{:class => css_classes_for(page)}
        - haml_tag (defined?(heading) ? heading : :h2) do
          %a{ :href =>  url_for(page) }<
            = page.heading
          %a.permalink{:href => page.abspath} ∞
        ~ page.summary



